Implement the following based on the existing plan: $ARGUMENTS

Follow the Implementer role from .agent-context/AGENTS.md:

1. Read the plan from dev-docs/{feature}/plan.md if it exists
2. Read .agent-context/MEMORY.md for relevant context and gotchas
3. Create a new git branch for this work if not already on one
4. Use TodoWrite to break the implementation into trackable steps
5. Implement changes following the plan step by step
6. Run tests after each significant change
7. Run the linter to catch style issues
8. Update .agent-context/MEMORY.md with any new learnings or gotchas discovered

Do NOT commit or push — leave that for the /review and /publish steps.
